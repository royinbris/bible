# 가톨릭 성경 웹 애플리케이션 파일 가이드 (Files Guide)

이 문서는 `app` 디렉토리 내에 존재하는 모든 폴더의 역할과 개별 파일들의 구체적인 기능 및 구현 내용을 상세히 설명합니다.

---

## 1. 디렉토리 구조 요약 (Directory Structure)

```text
app/
├── api/                   # 서버 사이드 API 엔드포인트 (Serverless Functions)
├── public/                # 정적 리소스 (아이콘, 기도문 데이터 파일 등)
├── src/                   # 프런트엔드 애플리케이션 소스 코드
│   ├── assets/            # 이미지 및 정적 에셋
│   ├── components/        # 공통 재사용 컴포넌트 및 바텀 시트
│   ├── context/           # 전역 상태 관리 컨텍스트 (React Context)
│   ├── hooks/             # 커스텀 React 훅
│   ├── lib/               # 비즈니스 로직 헬퍼 및 메타데이터
│   ├── pages/             # 주요 라우트별 페이지 화면
│   ├── App.css            # 글로벌 공통 스타일
│   ├── App.jsx            # 메인 라우터 및 하단 막대(GlobalBottomBar) 구현체
│   ├── index.css          # 디자인 시스템 토큰 및 공통 테마 정의
│   └── main.jsx           # 애플리케이션 진입점 (Vite Entry Point)
├── tests/                 # Playwright E2E 테스트 스펙 폴더
└── 루트 설정 파일들         # package.json, vite.config.js, eslint.config.js 등
```

---

## 2. 폴더별 상세 역할 및 파일 설명

### 📁 `app/api/` (서버 API 폴더)
서버리스 환경(Vercel 등)에서 동작하며, 외부 천주교 사이트의 정보를 실시간으로 크롤링/파싱하여 프런트엔드에 깔끔한 JSON 규격으로 반환하는 역할을 수행합니다.

*   **`mass.js`**
    *   **역할**: 매일미사 실시간 파싱 API
    *   **내용**: 요청 파라미터 `date` (YYYYMMDD 형식) 및 `type` (`ko` 한글 / `en` 영어)을 기반으로 동작합니다. 한글 미사는 주교회의 매일미사 사이트(`missa.cbck.or.kr`)를, 영어 미사는 `Universalis` 사이트를 크롤링하여 독서1, 독서2, 복음 구절 메타데이터(도서 ID, 장, 절 범위 등)와 오늘의 묵상 텍스트를 구조화된 JSON 데이터로 파싱해 전달합니다.
*   **`mass-html.js`**
    *   **역할**: 매일미사 본문 HTML 프록시 API
    *   **내용**: 매일미사 본문 상세 내용(HTML 양식)을 프런트엔드에서 크로스 오리진(CORS) 제한 없이 안전하게 iframe에 불러와서 렌더링할 수 있도록 중계해 주는 프록시 역할을 합니다.

---

### 📁 `app/src/components/` (재사용 공통 컴포넌트)
앱 전체에서 공유되어 여러 화면 위에 오버레이 형식으로 뜨는 UI 바텀 시트 및 보조 유틸리티성 컴포넌트들을 포함합니다.

*   **`ErrorBoundary.jsx`**
    *   **역할**: 애플리케이션 전역 에러 감지 컴포넌트
    *   **내용**: 하위 렌더링 중 예기치 못한 JS 에러가 발생했을 때 하얀 화면으로 멈추는 현상을 방지하고, 에러 상황을 유려하게 안내하며 홈 화면으로의 복귀를 유도하는 Fallback UI를 제공합니다.
*   **`HistorySheet.jsx`**
    *   **역할**: 성경 읽기 기록 및 책갈피 바텀 시트
    *   **내용**: 사용자가 읽었던 성경 장/절의 히스토리 내역(최근 읽은 기록 배지 표시 및 고정 핀 기능 제공)과 사용자가 하이라이트하여 저장해둔 '책갈피(나의 구절)'를 탭 분할 방식으로 관리하며, 클립보드 복사 및 즉시 해당 구절로의 라우팅 이동을 지원합니다.
*   **`SettingsSheet.jsx`**
    *   **역할**: 환경설정 시트 컴포넌트
    *   **내용**: 글꼴 크기 변경, 배경 테마 변경(라이트 모드, 올리브/블루 세피아, 다크 모드), TTS 낭독 시 배속 설정(최대 1.5배속 제한) 등의 전역 스타일 설정을 조절하는 인터페이스 컴포넌트입니다.

---

### 📁 `app/src/context/` (전역 상태 및 데이터 공급자)
성경 데이터 로드와 TTS 조작 핸들러, 미사 연동 및 설정 유지보수를 위한 전역 상태 컨텍스트입니다.

*   **`BibleContext.jsx`**
    *   **역할**: 성경 및 미사/기도의 핵심 비즈니스 상태 관리 및 로컬 데이터베이스 조작
    *   **내용**: `IndexedDB` 또는 `localforage`에 담긴 전체 성경 번역 데이터 로딩 상태를 주관합니다. 사용자의 마지막 독서 위치(`continueReadPos`), TTS 음성 낭독에 관한 전역 플레이어 상태 제어 핸들러, 매일미사 연동 오버레이(`massOverlay`) 데이터 바인딩, 그리고 기도 카테고리 필터값 등 서비스 전반을 관통하는 상태값들의 전역 스토어 역할을 담당합니다.
*   **`SettingsContext.jsx`**
    *   **역할**: 앱 환경설정 영속화 컨텍스트
    *   **내용**: `localStorage`에 사용자의 환경설정(테마, 글자 크기, 글꼴 스타일, 낭독 속도 등)을 안전하게 읽고 쓰며, CSS 변수(`--bg-color`, `--text-color` 등)를 HTML 루트 엘리먼트에 동적으로 주입하여 앱 테마를 즉각 반영시킵니다.

---

### 📁 `app/src/hooks/` (커스텀 훅)
*   **`useSimpleTTS.js`**
    *   **역할**: 브라우저 내장 Web Speech API 기반 음성 낭독 훅
    *   **내용**: HTML5 SpeechSynthesis 인터페이스를 리액트 생명주기에 맞게 바인딩하여 재생, 일시정지, 이전/다음 구절 건너뛰기, 정지 동작을 간편히 제어하며, 현재 낭독 중인 구절 ID를 반환하여 텍스트에 하이라이트 애니메이션이 표시될 수 있도록 돕습니다.

---

### 📁 `app/src/lib/` (라이브러리 및 정적 데이터 설정)
*   **`bibleInfo.js`**
    *   **역할**: 성경 메타데이터 정의 파일
    *   **내용**: 구약(46권) 및 신약(27권)의 정규 책 이름, 영어 표기, 2글자 약어 정보, 각 권별 전체 장의 수, IndexedDB 저장용 DB Key 상수를 선언하고 맵 형태로 매핑해두어 다른 화면에서 성경 정보를 참조할 때 중추가 되는 기초 메타데이터 파일입니다.

---

### 📁 `app/src/pages/` (화면단 라우트 페이지 컴포넌트)
*   **`Home.jsx`**
    *   **역할**: 신앙 생활의 메인 대시보드
    *   **내용**: 오늘 날짜를 기준으로 '오늘의 한권읽기' 목표량, '오늘의 매일미사' 독서/복음 구절 정보(API 호출 연동), 현재 아침/낮/저녁 시간대에 맞는 '나의 추천 기도문' 리스트를 보기 쉽게 대시보드 카드로 제공하여 개인 신앙 통독 흐름을 잇는 관문 역할을 수행합니다.
*   **`BibleList.jsx`**
    *   **역할**: 성경 73권의 목록 브라우저
    *   **내용**: 구약성경(율법서, 역사서, 시서와 지혜서, 예언서)과 신약성경(복음서, 역사서, 서간, 묵시록)을 장르별로 깔끔하게 그룹핑하여 원하는 성경 책을 빠르게 탐색해 들어갈 수 있는 그리드형 네비게이터 화면입니다.
*   **`ChapterList.jsx`**
    *   **역할**: 성경 권별 장 선택 목록 화면
    *   **내용**: 예: '창세기'를 선택하고 들어왔을 때 창세기에 내포된 1장부터 50장까지의 카드 목록을 유려하게 렌더링하며, 각 장별로 등록된 대표 소제목(Subheading)들을 장 번호 하단에 작게 프리뷰하여 읽고 싶은 주제를 골라 바로 독서 화면으로 진입할 수 있도록 돕습니다.
*   **`Reader.jsx`**
    *   **역할**: 핵심 성경 텍스트 독서 및 무한 스크롤 뷰어
    *   **내용**: 선택한 성경의 장/절을 스크롤하여 위아래로 내릴 때 앞뒤 장이 디스크 병목 없이 선제적으로 백그라운드 프리로드(IntersectionObserver API)되어 상하 무한 스크롤링됩니다. 또한 낭독 중인 구절의 TTS 연동 하이라이트 강조, 개별 절 선택 시 책갈피 저장 팝업 노출 등 완성도 높은 독서 인터페이스를 구현합니다.
*   **`Search.jsx`**
    *   **역할**: 고성능 성경 텍스트 전문(Full-Text) 검색기
    *   **내용**: IndexedDB 내 전체 성경 텍스트를 로컬 인메모리 필터 검색하여, 특정 검색 키워드가 포함된 성경 구절을 실시간으로 가져옵니다. 구약/신약/특정 성경책 필터를 제공하며 검색 결과가 본문 뷰어와 직접 연결됩니다.
*   **`DailyMass.jsx`**
    *   **역할**: 매일미사 상세 뷰어 및 성경 구절 오버레이 리더
    *   **내용**: 주교회의에서 제공하는 오늘의 미사 예식문을 렌더링하며, 미사 독서나 복음 텍스트에 언급되는 특정 성경 구절 링크(예: "창세 1,3")를 탭하면 앱을 벗어나지 않고 화면 하단에서 슬라이드 오버레이 창으로 해당 성경의 전체 장/절 텍스트를 즉시 로딩해 띄워주는 파워풀한 신앙 학습 기능을 제공합니다.
*   **`BibleReadingPlan.jsx`**
    *   **역할**: 나만의 한권읽기(성경 통독) 플래너
    *   **내용**: 사용자가 설정한 기간(예: 3개월, 6개월) 동안 특정 성경책 혹은 성경 전체를 완독할 수 있도록 일일 통독 일정을 자동 배분하여 표로 생성해 줍니다. 완료 여부를 직접 체크하고 남은 분량을 한눈에 진행률 프로그레스 바 그래프로 확인할 수 있습니다.
*   **`PrayersList.jsx`**
    *   **역할**: 가톨릭 기도문 카테고리 목록 및 추천 홈
    *   **내용**: 주요기도, 일상기도, 신심기도, 전구기도 등 텍스트 파일(`prayers.md`)을 읽어와서 파싱하여 카테고리별 목록으로 제공합니다. 전체 기도문 실시간 필터링 검색을 지원합니다.
*   **`PrayersDetail.jsx`**
    *   **역할**: 기도문 본문 전용 독서실 화면
    *   **내용**: 선택한 기도문의 본문 텍스트를 깔끔한 서체와 낭독 TTS 오디오 제어 기능, 폰트 조절 환경을 연동하여 편안하게 묵상하며 기도할 수 있게 지원하는 상세 뷰어 화면입니다.

---

### 📁 `app/tests/` (테스트 환경)
*   **`e2e.spec.js`**
    *   **역할**: Playwright 기반 종단간 E2E 시나리오 스펙
    *   **내용**: 홈 화면 진입 시 대시보드 렌더링 여부 검증, 기도 목록 및 카테고리 필터 탭 조작 테스트, 가상 미사 API 응답 데이터를 Mocking 주입하여 매일미사 오버레이 리더의 무한 루프 방지 로직과 영문 미사 본문 조회 기능이 무결하게 실행되는지 검사하는 자동화 테스트입니다.

---

## 3. 루트 주요 진입 파일

*   **`app/src/App.jsx`**
    *   **역할**: 프런트엔드 애플리케이션의 루트(Root) 제어 및 레이아웃 바
    *   **내용**: 전체 페이지의 라우터 구조(`react-router-dom` 활용)를 총괄하며, 모바일 최적화를 위해 손가락 스와이프 제스처를 통한 뒤로 가기 / 앞으로 가기 브라우징 탐색 기능과 화면 삼중 탭 시 전체 화면 전환 모드를 처리합니다. 하단에 고정 표시되는 공통 막대(`GlobalBottomBar`) 컴포넌트를 내포하고 있어, 각 도메인 상태에 맞는 기본 메뉴(5탭) 및 개별 세부 메뉴 간 전환을 매끄럽게 처리합니다.
*   **`app/src/index.css`**
    *   **역할**: 브랜드 아이덴티티 및 디자인 스타일 시스템 토큰 정의
    *   **내용**: 세련되고 깊이 있는 디자인을 위해 HSL 기반 컬러 팔레트(--primary-color, --nav-bg, --card-bg 등)를 다크 모드와 올리브 테마별로 정의하고, 모바일 스와이프 반동 방지 스타일 및 전역 서체 지정을 관장합니다.
